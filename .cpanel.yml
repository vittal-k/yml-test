---
deployment:
      tasks:
        - export DEPLOYPATH=/home/craftedhire/public_html  # Add /<app_folder> if required
        - /bin/cp -r * $DEPLOYPATH                        #copy all recursively to $DEPLOYPATH
